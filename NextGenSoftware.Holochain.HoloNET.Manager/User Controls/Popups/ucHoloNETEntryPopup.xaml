<UserControl x:Class="NextGenSoftware.Holochain.HoloNET.Manager.UserControls.ucHoloNETEntryPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:local="clr-namespace:NextGenSoftware.Holochain.HoloNET.Manager.UserControls"
             xmlns:uc="clr-namespace:NextGenSoftware.Holochain.HoloNET.Manager.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/NextGenSoftware.Holochain.HoloNET.Manager;component/Resources/Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Opacity="0.9" Grid.Row="0" Grid.RowSpan="6" Width="1000">
        <Grid Background="Transparent" Opacity="0.5"/>
        <Border Width="1000"
                    Height="440"
                Background="DarkBlue" 
                Opacity="50"
                BorderBrush="Black" 
                BorderThickness="1" 
                CornerRadius="0,55,0,55" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Margin="10,20,10,20" Text="HoloNET Data Entry (Internal Connection)" HorizontalAlignment="Center" Foreground="WhiteSmoke" FontWeight="Bold" FontSize="20"  />

                <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
                    <Button IsEnabled="False" x:Name="btnViewHoloNETEntry" Content="Entry" Style="{StaticResource ButtonStyle}" Cursor="Hand" Margin="10" Foreground="WhiteSmoke" Width="270" FontSize="16" Background="{x:Null}" Click="btnViewHoloNETEntry_Click">
                        <Button.Effect>
                            <DropShadowEffect x:Name="btnViewHoloNETEntryGlowEffect" ShadowDepth="0"
Color="White"
Opacity="0"
BlurRadius="10"/>
                        </Button.Effect>

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
      Storyboard.TargetName="btnViewHoloNETEntryGlowEffect" 
      Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
      From="0.0" To="1.0" Duration="0:0:0.5" 
      AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
      Storyboard.TargetName="btnViewHoloNETEntryGlowEffect" 
      Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
      From="1" To="0" Duration="0:0:0.5" 
      AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                    <Button x:Name="btnViewHoloNETEntryMetaData" Content="Meta Data" Style="{StaticResource ButtonStyle}" Cursor="Hand" Margin="10" Foreground="WhiteSmoke" Width="270" FontSize="16" Background="{x:Null}" Click="btnViewHoloNETEntryMetaData_Click">
                        <Button.Effect>
                            <DropShadowEffect x:Name="btnViewHoloNETEntryMetaDataGlowEffect" ShadowDepth="0"
Color="White"
Opacity="0"
BlurRadius="10"/>
                        </Button.Effect>

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
      Storyboard.TargetName="btnViewHoloNETEntryMetaDataGlowEffect" 
      Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
      From="0.0" To="1.0" Duration="0:0:0.5" 
      AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
      Storyboard.TargetName="btnViewHoloNETEntryMetaDataGlowEffect" 
      Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
      From="1" To="0" Duration="0:0:0.5" 
      AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                    <Button x:Name="btnViewHoloNETEntryInfo" Content="Information" Style="{StaticResource ButtonStyle}" Cursor="Hand" Margin="10" Foreground="WhiteSmoke" Width="270" FontSize="16" Background="{x:Null}" Click="btnViewHoloNETEntryInfo_Click">
                        <Button.Effect>
                            <DropShadowEffect x:Name="btnViewHoloNETEntryInfoGlowEffect" ShadowDepth="0"
Color="White"
Opacity="0"
BlurRadius="10"/>
                        </Button.Effect>

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
      Storyboard.TargetName="btnViewHoloNETEntryInfoGlowEffect" 
      Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
      From="0.0" To="1.0" Duration="0:0:0.5" 
      AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
      Storyboard.TargetName="btnViewHoloNETEntryInfoGlowEffect" 
      Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
      From="1" To="0" Duration="0:0:0.5" 
      AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                </StackPanel>

                <uc:ucHoloNETEntry Margin="0,0,0,0" x:Name="ucHoloNETEntry" Height="220"></uc:ucHoloNETEntry>
                <uc:ucHoloNETEntryMetaData x:Name="ucHoloNETEntryMetaData" Height="220" Visibility="Collapsed"></uc:ucHoloNETEntryMetaData>

                <StackPanel Orientation="Vertical" x:Name="stkpnlInfoHoloNETEntry" Visibility="Collapsed" Height="220">
                    <ScrollViewer Margin="10" Height="195">
                        <StackPanel Orientation="Vertical" Margin="10">
                            <TextBlock Foreground="WhiteSmoke" FontSize="16" TextWrapping="WrapWithOverflow" Text="This popup (ucHoloNETEntryPopup.xaml) shows how to easily extend the HoloNETEntryBase or HoloNETAuditEntryBase class in HoloNET ORM allowing you to perform simple CRUD operations such as Load, Save &amp; Delete without having to worry about the lower complexities of Holochain or configure and use the Holochain Conductor or API's. The HoloNET Entry class (Avatar) that extends the HoloNETAuditEntryBase class is configured to use it's own internal HoloNET Client so no existing client needs to be injected in. It will automatically create it's own internal client and then dispose of it when the HoloNET Entry is closed by calling the Close method on it. As the default, it will also automatically start and shutdown the Holochain Conductor, but in this example it has been configured using the HoloNETDNA injected in (DI) via the constructor of the HoloNET Entry (Avatar) class to not auto-start or shutdown the conductor because HoloNET Manager has already done this for us. When you use the HoloNETEntryBase/HoloNETAuditEntryBase class in a standalone app, site, game etc then you would leave this on the default to auto-start and shutdown the conductor."></TextBlock>
                            <TextBlock Margin="0,10,0,0" FontSize="16" Foreground="WhiteSmoke" TextWrapping="WrapWithOverflow" Text="The new HoloNET ORM (Object Relational Mapping) allows very easy &amp; rapid development of hApps. This consists of the HoloNETEntryBase, HoloNETAuditEntryBase, HoloNETCollection and HoloNETObservsableCollection classes. The first two can be extended to create your HoloNET Entry models that map directly onto your data structs in your rust hApp zome code. The second two are collections of these entries. You can then simply call basic CRUD methods on your classes such as Load, Save, Delete, etc making it very quick and easy to build hApps without having to worry about the lower complexitites of Holochain or configure and use the Holochain Conductor or API's. You also get change tracking, version control &amp; rollback functionality for free. HoloNET ORM allows any existing .net app/website/game/service to be rapidly and quickly converted to a Holochain hApp with very little code changes (you can simply add a new attribute to the properties in your models) and it will now map to your rust hApp structs (which WEB5 STAR ODK can also dynamically generate (it generates rust and c# code)."></TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" Margin="0,10,0,10" HorizontalAlignment="Center">
                    <Button x:Name="btnHoloNETEntryPopupCancel" Width="270" Style="{StaticResource ButtonStyle}" Cursor="Hand" Margin="10" Foreground="WhiteSmoke" FontSize="16" Content="Close" Background="{x:Null}" Click="btnHoloNETEntryPopupCancel_Click">
                        <Button.Effect>
                            <DropShadowEffect x:Name="btnHoloNETEntryPopupCancellGlowEffect" ShadowDepth="0"
              Color="White"
              Opacity="0"
              BlurRadius="10"/>
                        </Button.Effect>

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                    Storyboard.TargetName="btnHoloNETEntryPopupCancellGlowEffect" 
                    Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
                    From="0.0" To="1.0" Duration="0:0:0.5" 
                    AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                    Storyboard.TargetName="btnHoloNETEntryPopupCancellGlowEffect" 
                    Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
                    From="1" To="0" Duration="0:0:0.5" 
                    AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                    <Button x:Name="btnHoloNETEntryPopupDelete" Margin="10" Style="{StaticResource ButtonStyle}" Cursor="Hand" Foreground="WhiteSmoke" Width="270" FontSize="16" Content="Delete" Background="{x:Null}" Click="btnHoloNETEntryPopupDelete_Click">
                        <Button.Effect>
                            <DropShadowEffect x:Name="btnHoloNETEntryPopupDeleteEffect" ShadowDepth="0"
Color="White"
Opacity="0"
BlurRadius="10"/>
                        </Button.Effect>

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
          Storyboard.TargetName="btnHoloNETEntryPopupDeleteEffect" 
          Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
          From="0.0" To="1.0" Duration="0:0:0.5" 
          AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
          Storyboard.TargetName="btnHoloNETEntryPopupDeleteEffect" 
          Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
          From="1" To="0" Duration="0:0:0.5" 
          AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                    <Button x:Name="btnHoloNETEntryPopupSave" Margin="10" Style="{StaticResource ButtonStyle}" Cursor="Hand" Foreground="WhiteSmoke" Width="270" FontSize="16" Content="Save" Background="{x:Null}" Click="btnHoloNETEntryPopupSave_Click">
                        <Button.Effect>
                            <DropShadowEffect x:Name="btnHoloNETEntryPopupSaveGlowEffect" ShadowDepth="0"
          Color="White"
          Opacity="0"
          BlurRadius="10"/>
                        </Button.Effect>

                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                    Storyboard.TargetName="btnHoloNETEntryPopupSaveGlowEffect" 
                    Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
                    From="0.0" To="1.0" Duration="0:0:0.5" 
                    AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                    Storyboard.TargetName="btnHoloNETEntryPopupSaveGlowEffect" 
                    Storyboard.TargetProperty="(DropShadowEffect.Opacity)"
                    From="1" To="0" Duration="0:0:0.5" 
                    AutoReverse="False" RepeatBehavior="1x" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
